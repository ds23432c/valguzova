# ── Копируй в .env и заполни данными из Railway ──────────────

PORT=5000

# MySQL — берёшь из Railway → MySQL → Variables
DB_HOST=mysql.railway.internal
DB_PORT=3306
DB_USER=root
DB_PASSWORD=AfHrkInylWqdWjGHLDvPhBMnBZauoEDr
DB_NAME=railway

# JWT — придумай длинную строку (минимум 32 символа)
JWT_SECRET=kvizoria_super_secret_2024_change_this_please

# Email — для сброса пароля (необязательно при разработке)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# NODE_ENV
NODE_ENV=production
